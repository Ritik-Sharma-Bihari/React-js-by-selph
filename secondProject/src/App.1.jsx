import { useState } from 'react';

export function App() {
    const [count, setCount] = useState(0);

    return (
        <>
            <h1>this is simple project on counting the number of clikched</h1>
            <button type="">increse </button>
            <button type="" />
        </>
    );
}
